import { Box } from "@mui/material";
import { addProductModalStyle } from "./addProductModalStyle";

const AddProductModal:React.FC = (): JSX.Element => {
    return(
        <Box sx={addProductModalStyle.container}>
            <p> lol </p>
        </Box>
    )
};

export default AddProductModal